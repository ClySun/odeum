# macOS
.DS_Store

# Editor / tooling
.claude/

# Original photo files (kept locally as backups; the site uses the copies in /images)
/_.webp
/*.jpeg

# Original character art PNGs (kept locally; web uses the JPGs in images/prague/cast)
/Character art/

# Original host photo (kept locally; web uses the cropped images/prague/clytze.jpg)
/images/clytze.jpeg
